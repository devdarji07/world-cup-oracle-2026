"use client";
import{useApi}from "../../../hooks/useApi";import type{Standing}from "../../../types";
export default function ThirdPlacePage(){const{data,error}=useApi<Standing[]>("/best-third-teams");return <div className="mx-auto max-w-5xl"><p className="eyebrow">Qualification pressure</p><h1 className="section-title my-5">Best of third.</h1><p>{error}</p><div className="grid gap-4 md:grid-cols-2">{data?.map(t=><article key={t.team} className={`glass-card p-6 ${t.qualified?"border-cyan-300":"opacity-45"}`}><p className="eyebrow">Rank {t.third_place_rank} / Group {t.group}</p><div className="mt-3 flex justify-between"><h2 className="text-2xl font-black">{t.team}</h2><strong className="text-3xl text-cyan-300">{t.points}</strong></div><p className="mt-4 text-sm text-slate-400">GD {t.goal_difference} · {t.qualified?"QUALIFIED":"ELIMINATED"}</p></article>)}</div></div>}
