"use client";
import{motion}from "framer-motion";import{useApi}from "../../../hooks/useApi";import type{Odds}from "../../../types";
export default function OddsPage(){const{data,error}=useApi<Odds[]>("/champion-odds");return <div className="mx-auto max-w-6xl"><p className="eyebrow">10,000 simulated worlds</p><h1 className="section-title my-5">Champion odds.</h1><p>{error}</p><div className="space-y-4">{data?.map((t,i)=><motion.article key={t.team} initial={{opacity:0,x:-30}} animate={{opacity:1,x:0}} transition={{delay:i*.03}} className="glass-card p-6"><div className="flex items-end justify-between"><div><span className="eyebrow">#{i+1} · Final {t.final_probability}%</span><h2 className="mt-2 text-2xl font-black">{t.team}</h2></div><strong className="text-3xl text-cyan-300">{t.champion_probability}%</strong></div><div className="mt-5 h-3 overflow-hidden rounded-full bg-white/10"><div className="h-full bg-gradient-to-r from-violet-500 to-cyan-300" style={{width:`${Math.max(t.champion_probability*4,1)}%`}}/></div></motion.article>)}</div></div>}
