import type { Match } from "../types";
export function MatchCard({match}:{match:Match}){return <article className="glass-card p-5"><div className="mb-4 flex justify-between text-xs font-bold uppercase tracking-widest text-cyan-300"><span>{match.match_id}</span><span>{match.stage}</span></div><div className="grid grid-cols-[1fr_auto_1fr] items-center gap-3 text-lg font-black"><span>{match.home_team}</span><span className="score">{match.predicted_score}</span><span className="text-right">{match.away_team}</span></div><p className="mt-4 text-center text-xs font-black uppercase tracking-widest text-amber-300">{match.winner ? `Winner: ${match.winner}` : "Predicted draw"}</p><p className="mt-3 text-xs text-slate-400">{match.home_win_probability}% / {match.draw_probability}% / {match.away_win_probability}%</p></article>}
